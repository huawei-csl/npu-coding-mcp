# __ull2half_rna

> **Section**: 6.3.9.196  
> **PDF Pages**: 3595–3595  

---

<!-- page 3595 -->

需要包含的头文件

使用该接口需要包含"simt_api/asc_fp16.h"头文件。

```cpp
#include "simt_api/asc_fp16.h"
```

调用示例

SIMD与SIMT混合编程场景：__simt_vf__ __launch_bounds__(1024) inline void kernel__ull2half_ru(__gm__ half* dst, __gm__ uint64_t* x){    int idx = threadIdx.x + blockIdx.x * blockDim.x;    dst[idx] = __ull2half_ru(x[idx]);}

## 6.3.9.196 __ull2half_rna

产品支持情况

产品是否支持

Atlas 350 加速卡√

Atlas A3 训练系列产品/Atlas A3 推理系列产品x

Atlas A2 训练系列产品/Atlas A2 推理系列产品x

Atlas 200I/500 A2 推理产品x

Atlas 推理系列产品AI Corex

Atlas 推理系列产品Vector Corex

Atlas 训练系列产品x

功能说明

遵循CAST_ROUND模式，将uint64类型数据转换为half类型数据，返回转换后的值。

函数原型

```cpp
inline half __ull2half_rna(const unsigned long long int x)
```

参数说明

表6-1831参数说明

参数名输入/输出

描述

x输入源操作数。
