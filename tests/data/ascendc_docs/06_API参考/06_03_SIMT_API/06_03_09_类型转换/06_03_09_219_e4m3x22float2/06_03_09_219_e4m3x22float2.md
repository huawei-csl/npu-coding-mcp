# __e4m3x22float2

> **Section**: 6.3.9.219  
> **PDF Pages**: 3622–3623  

---

<!-- page 3622 -->

参数说明

表6-1853参数说明

参数名输入/输出

描述

x输入源操作数。

返回值说明

将hifloat8x2_t类型输入转换成的half2类型数据。

约束说明

SIMT编程场景当前不支持使用该接口。

需要包含的头文件

使用该接口需要包含"simt_api/asc_fp8.h"头文件。

```cpp
#include "simt_api/asc_fp8.h"
```

调用示例

SIMD与SIMT混合编程场景：// 使用小向量可提升数据搬运效率__simt_vf__ __launch_bounds__(1024) inline void simt_hif822half2(__gm__ hifloat8x2_t* input, __gm__ half2* output, uint32_t input_total_length){    uint32_t idx = blockIdx.x * blockDim.x + threadIdx.x;    // 每个线程处理1个hifloat8x2_t类型的数据，即2个hifloat8_t类型的数据，因此idx >= input_total_length / 2的线程不处理数据    if (idx > input_total_length /2) {        return;    }    output[idx] = __hif822half2(input[idx]);}__global__ __vector__ void cast_kernel(__gm__ uint8_t* input,  __gm__ half* output, uint32_t input_total_length){    asc_vf_call<simt_hif822half2>(dim3(1024), (__gm__ hifloat8x2_t*)input, (__gm__ half2*)output, input_total_length);}

## 6.3.9.219 __e4m3x22float2

产品支持情况

产品是否支持

Atlas 350 加速卡√

Atlas A3 训练系列产品/Atlas A3 推理系列产品x

Atlas A2 训练系列产品/Atlas A2 推理系列产品x

<!-- page 3623 -->

产品是否支持

Atlas 200I/500 A2 推理产品x

Atlas 推理系列产品AI Corex

Atlas 推理系列产品Vector Corex

Atlas 训练系列产品x

功能说明

将float8_e4m3x2_t类型数据的两个分量转换为浮点数，返回转换后的float2类型数据。

函数原型

```cpp
inline float2 __e4m3x22float2(const float8_e4m3x2_t x)
```

参数说明

表6-1854参数说明

参数名输入/输出

描述

x输入源操作数。

返回值说明

将float8_e4m3x2_t类型输入转换成的float2类型数据。

约束说明

SIMT编程场景当前不支持使用该接口。

需要包含的头文件

使用该接口需要包含"simt_api/asc_fp8.h"头文件。

```cpp
#include "simt_api/asc_fp8.h"
```

调用示例

SIMD与SIMT混合编程场景：// 使用小向量可提升数据搬运效率__simt_vf__ __launch_bounds__(1024) inline void simt_e4m3x22float2(__gm__ float8_e4m3x2_t* input, __gm__ float2* output, uint32_t input_total_length){    uint32_t idx = blockIdx.x * blockDim.x + threadIdx.x;    // 每个线程处理1个float8_e4m3x2_t类型的数据，即2个fp8_e4m3fn_t类型的数据，因此idx >= input_total_length / 2的线程不处理数据    if (idx > input_total_length /2) {
