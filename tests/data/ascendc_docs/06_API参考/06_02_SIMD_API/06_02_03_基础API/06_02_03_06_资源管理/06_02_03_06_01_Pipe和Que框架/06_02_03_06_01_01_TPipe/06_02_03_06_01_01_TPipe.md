# TPipe

> **Section**: 6.2.3.6.1.1  
> **PDF Pages**: 1733–1733  

---

<!-- page 1733 -->

参数说明

表6-658模板参数说明

参数名描述

T操作数的数据类型。

Atlas 350 加速卡，支持的数据类型为：int8_t、uint8_t、int16_t、uint16_t、int32_t、uint32_t、int64_t、uint64_t。

表6-659接口参数说明

参数名输入/输出

含义

addr输入源GM地址。

返回值说明

源GM地址上的数据。

约束说明

连续使用（Polling）该接口，用于进行多核同步，会造成Bus被长期占用，导致其他核无法访问该GM，导致多核读同一GM出现卡死现状，若需要Polling，建议在该使用接口间，增加Nop指令，指令量建议为800个，一般为需要同步的核数 * 200。

调用示例

```cpp
__gm__ int32_t* addr = srcGlobal.GetPhyAddr();int32_t value = ReadGmByPassDCache(addr);
```

// 通过Scalar读写相同GM时，需要让读操作，空出总线，避免长时占用int target = -1;while (target != 0) {     target = ReadGmByPassDCache(addr);     Nop<800>();}

## 6.2.3.6 资源管理

## 6.2.3.6.1 Pipe 和Que 框架

## 6.2.3.6.1.1 TPipe

## ?.1. TPipe 简介

TPipe用于统一管理Device端内存等资源，一个Kernel函数必须且只能初始化一个TPipe对象。其主要功能包括：
