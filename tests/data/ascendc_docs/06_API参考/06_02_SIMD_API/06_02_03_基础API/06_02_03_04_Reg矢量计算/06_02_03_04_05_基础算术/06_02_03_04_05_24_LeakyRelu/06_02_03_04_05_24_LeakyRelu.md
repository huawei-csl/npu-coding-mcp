# LeakyRelu

> **Section**: 6.2.3.4.5.24  
> **PDF Pages**: 1604–1605  

---

<!-- page 1604 -->

表6-545参数说明

参数名输入/输出

描述

dstReg输出目的操作数。

类型为RegTensor。

srcReg输入源操作数。

类型为RegTensor。

数据类型需要与目的操作数保持一致。

scalarValue输入源操作数。

类型为RegTensor。

数据类型需要与目的操作数保持一致。

mask输入源操作数元素操作的有效指示，详细说明请参考MaskReg。

返回值说明

无

约束说明

无

调用示例

```cpp
template<typename T>__simd_vf__ inline void MinsVF(__ubuf__ T* dstAddr, __ubuf__ T* src0Addr, T scalarValue, uint32_t count, uint32_t oneRepeatSize, uint16_t repeatTimes){    AscendC::Reg::RegTensor<T> srcReg;
    AscendC::Reg::RegTensor<T> dstReg;
    AscendC::Reg::MaskReg mask;
        for (uint16_t i = 0;
 i < repeatTimes;
 i++) {        mask = AscendC::Reg::UpdateMask<T>(count);
        AscendC::Reg::LoadAlign(srcReg, src0Addr + i * oneRepeatSize);
        AscendC::Reg::Mins(dstReg, srcReg, scalarValue, mask);
        AscendC::Reg::StoreAlign(dstAddr + i * oneRepeatSize, dstReg, mask);    }}
```

## 6.2.3.4.5.24 LeakyRelu

产品支持情况

产品是否支持

Atlas 350 加速卡√

Atlas A3 训练系列产品/Atlas A3 推理系列产品x

Atlas A2 训练系列产品/Atlas A2 推理系列产品x

<!-- page 1605 -->

产品是否支持

Atlas 200I/500 A2 推理产品x

Atlas 推理系列产品AI Corex

Atlas 推理系列产品Vector Corex

Atlas 训练系列产品x

功能说明

源操作数srcReg大于0的情况下直接将srcReg写入目的操作数dstReg，否则将srcReg*scalarValue的结果写入dstReg。计算公式如下：

![p1605_img001.png](../../../../images/p1605_img001.png)

函数原型

```cpp
template <typename T = DefaultType, typename U, MaskMergeMode mode = MaskMergeMode::ZEROING, typename S>__simd_callee__ inline void LeakyRelu(S& dstReg, S& srcReg, U scalarValue, MaskReg& mask)
```

参数说明

表6-546模板参数说明

参数名描述

T目的操作数和源操作数的数据类型。

Atlas 350 加速卡，支持的数据类型为：half/float

U标量源操作数的数据类型。

Atlas 350 加速卡，支持的数据类型为：half/float

mode●选择MERGING模式或ZEROING模式。

–ZEROING，mask未选中/未被映射的元素在dstReg中置零。

–MERGING，暂不支持。

S目的操作数的RegTensor类型，例如RegTensor<half>，由编译器自动推导，用户不需要填写。

表6-547参数说明

参数名输入/输出

描述

dstReg输出目的操作数。

类型为RegTensor。
