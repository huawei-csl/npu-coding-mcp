# 接口变更说明

> **Section**: 6.6.3  
> **PDF Pages**: 3905–3906  

---

<!-- page 3905 -->

std::vector<FieldInfo> GetFieldInfo() const;const char *GetTilingClassName() const;size_t GetDataSize() const;void SetDataPtr(void *dataPtr);void CheckAlignAndGenPlaceHolder(const char *name, size_t typeSize);// FieldInfo类FieldInfo(const char *dtype, const char *name)FieldInfo(const char *dtype, const char *name, size_t arrSize)FieldInfo(const char *dtype, const char *name, const char *structType,size_t structSize)

## OpLib 注册辅助接口

用于OpLib注册的辅助接口，包括OpBinInfo类及相关成员函数等，OpBinInfo类及相关成员函数均为框架生成相关工程所需，开发者无需关心。

类名接口名接口功能

OpBinInfoOpBinInfoOpBinInfo类的构造函数。

Generate解压自定义算子工程的库文件。

Check校验是否为动态库类型。

## 6.6.3 接口变更说明

为了优化API命名，使其更加清晰和易于理解，当前版本相比于CANN8.5.0及前序版本，部分API命名有变更，具体变更情况请参考以下表格：

原接口名新接口名备注

ScalarGetCountOfValue

**GetBitCount无**

ScalarCountLeadingZero

**CountLeadingZero无**

ScalarGetSFFValueGetSFFValue无

ScalarCastCast（float转half、int32_t）

无

ToBfloat16Cast（float转bfloat16_t）

无

ToFloatCast（多类型转float）

其中一个重载版本的返回值类型由float改为模板U, U = float

FusedMulAddReluMulAddRelu无

CastDeqCastDequant无

CompareScalarCompares无

GetAccValGetReduceRepeatSumSpr

无

<!-- page 3906 -->

原接口名新接口名备注

GetReduceMaxMinCount

**GetReduceRepeatMaxMinSpr**

无

SetAtomicNoneDisableDmaAtomic无

InitConstValueFill无

SetMMLayoutTransform

**SetMMRowMajor/SetMMColumnMajor**

SetMMLayoutTransform(true)对应SetMMRowMajor()。

SetMMLayoutTransform(false)对应SetMMColumnMajor()。

SetHF32ModeSetHF32Mode接口名不变，参数类型由bool修改为枚举HF32Mode。

SetHF32TransMode

**SetHF32TransMode接口名不变，参数类型由bool修改为枚举HF32TransMode。**

CalcTschBlockDimCalcTschNumBlocks无
