# erfcinvf

> **Section**: 6.3.4.71  
> **PDF Pages**: 3202–3203  

---

<!-- page 3202 -->

参数说明

表6-1524函数形参说明

参数名输入/输出

描述

x输入源操作数。

返回值说明

●当x为0时，返回值为0。

●当x为nan时，返回值为nan。

●当x为1时，返回值为inf。

●当x为-1时，返回值为-inf。

●当x∉[-1,1]时，返回nan。

约束说明

SIMT编程场景当前不支持使用该接口。

需要包含的头文件

使用该接口需要包含"simt_api/math_functions.h"头文件。

```cpp
#include "simt_api/math_functions.h"
```

调用示例

SIMD与SIMT混合编程场景：

```cpp
__simt_vf__ __launch_bounds__(1024) inline void KernelErfc(__gm__ float* x, __gm__ float* y){    int idx = threadIdx.x + blockIdx.x * blockDim.x;
    y[idx] = erfinvf(x[idx]);}
```

## 6.3.4.71 erfcinvf

产品支持情况

产品是否支持

Atlas 350 加速卡√

Atlas A3 训练系列产品/Atlas A3 推理系列产品x

Atlas A2 训练系列产品/Atlas A2 推理系列产品x

Atlas 200I/500 A2 推理产品x

Atlas 推理系列产品AI Corex

<!-- page 3203 -->

产品是否支持

Atlas 推理系列产品Vector Corex

Atlas 训练系列产品x

功能说明

获取输入数据的逆互补误差函数值。

![p3203_img001.png](../../images/p3203_img001.png)

函数原型

```cpp
inline float erfcinvf(float x)
```

参数说明

表6-1525函数形参说明

参数名输入/输出

描述

x输入源操作数。

返回值说明

●当x为0时，返回值为inf。

●当x为nan时，返回值为nan。

●当x为2时，返回值为-inf。

●当x∉[0,2]时，返回nan。

约束说明

SIMT编程场景当前不支持使用该接口。

需要包含的头文件

使用该接口需要包含"simt_api/math_functions.h"头文件。

```cpp
#include "simt_api/math_functions.h"
```

调用示例

SIMD与SIMT混合编程场景：__simt_vf__ __launch_bounds__(1024) inline void KernelErfc(__gm__ float* x, __gm__ float* y){    int idx = threadIdx.x + blockIdx.x * blockDim.x;    y[idx] = erfcinvf(x[idx]);}
