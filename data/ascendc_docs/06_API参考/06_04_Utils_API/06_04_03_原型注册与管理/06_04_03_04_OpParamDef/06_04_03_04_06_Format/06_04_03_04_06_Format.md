# Format

> **Section**: 6.4.3.4.6  
> **PDF Pages**: 3790–3790  

---

<!-- page 3790 -->

```cpp
class XxxCustom : public OpDef {public:    XxxCustom(const char* name) : OpDef(name)    {        this->Input("x")            .ParamType(REQUIRED)            .DataTypeList({ge::DT_FLOAT16})            .Format({ge::FORMAT_ND, ge::FORMAT_ND, ge::FORMAT_ND});
        this->Input("y")            .ParamType(REQUIRED)            .DataType({ge::DT_FLOAT16, ge::DT_FLOAT, ge::DT_INT32})            .Format({ge::FORMAT_ND, ge::FORMAT_ND, ge::FORMAT_ND});
        this->Output("z")            .ParamType(REQUIRED)            .DataType({ge::DT_FLOAT16, ge::DT_FLOAT, ge::DT_INT32})            .Format({ge::FORMAT_ND, ge::FORMAT_ND, ge::FORMAT_ND});        ...    }};
```

函数原型

```cpp
OpParamDef &DataTypeList(std::vector<ge::DataType> types)
```

参数说明

参数输入/输出说明

**types输入算子参数数据类型。**

返回值说明

OpParamDef算子定义，OpParamDef请参考6.4.3.4 OpParamDef。

约束说明

●同一输入/输出不能同时设置DataType和DataTypeList。

●本接口不支持和UnknownShapeFormat同时使用。

调用示例

```cpp
class AddCustom : public OpDef {public:    AddCustom(const char* name) : OpDef(name)    {        this->Input("x")            .ParamType(REQUIRED)            .DataTypeList({ge::DT_FLOAT})            .Format({ge::FORMAT_ND, ge::FORMAT_NCHW});
        this->Input("x1")             ......    }};
```

## 6.4.3.4.6 Format

功能说明

定义算子参数数据格式。
