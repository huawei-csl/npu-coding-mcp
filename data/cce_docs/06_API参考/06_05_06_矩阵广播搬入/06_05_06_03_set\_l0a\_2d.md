# set\_l0a\_2d

> **Section**: 6.5.6.3


## 功能说明

实现数据广播到 L0A 。将 L0\_SET\_VALUE 寄存器中的 16 位 /32 位的单个数据复制到目标 区域的所有指定块中，根据目标地址的类型决定 L0\_SET\_VALUE 寄存器中搬运的类型。 地址必须 512B 对齐。（ Block 大小为 512B ）

若 config[14:0] = 0 （重复次数 0 ）或 config[30:16] = 0 （每轮块数 0 ），接口视为 NOP （不执行）并报错。

// 相同接口的不同原型区别在于源地址和目的地址的数据类型不同 void set\_l0a\_2d(\_\_ca\_\_ float *dst, int64\_t config);

void set\_l0a\_2d(\_\_ca\_\_ uint16\_t *dst, int64\_t config);

void set\_l0a\_2d(\_\_ca\_\_ uint32\_t *dst, int64\_t config);

void set\_l0a\_2d(\_\_ca\_\_ int32\_t *dst, int64\_t config);

void set\_l0a\_2d(\_\_ca\_\_ bfloat16\_t *dst, int64\_t config);

void set\_l0a\_2d(\_\_ca\_\_ half *dst, int64\_t config);

void set\_l0a\_2d(\_\_ca\_\_ int16\_t *dst, int64\_t config);

## 参数含义见表 1 config 位数说明

PIPE\_MTE1
