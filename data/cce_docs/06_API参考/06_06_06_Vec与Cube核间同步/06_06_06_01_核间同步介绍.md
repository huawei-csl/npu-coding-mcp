# 核间同步介绍

> **Section**: 6.6.6.1


核间同步又称为硬同步。硬件同步机制仅适用于 AIC/AIV Mix 算子，不适用于纯 AIC 算 子或纯 AIV 算子。（ AIC 指 Cube ； AIV 指 Vector ）。


## 功能说明

## 接口原型

## 参数说明

## 流水类型

用于初始化 FFTS\_BASE\_ADDR 寄存器，参数为 FFTS base address ，需要在 Host 侧调用 rtGetC2cCtrlAddr 获取，并作为算子参数传入。

void rtGetC2cCtrlAddr(uint64\_t&amp; ffts\_address, uint32\_t&amp; ffts\_len);

void set\_ffts\_base\_addr(uint64\_t config);

## 表 6-36 参数说明

| 参数名    | 说明        | 取值范围         | 单位    |
|--------|-----------|--------------|-------|
| config | FFTS 起始地址 | [0 ， 2^48-1] | 4B 对齐 |

PIPE\_S